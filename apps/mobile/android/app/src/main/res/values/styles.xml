<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Las franjas de las barras del sistema, del color de la app y no
             blancas: con los márgenes de adjustMarginsForEdgeToEdge se ve la
             ventana por detrás. values-night pone la paleta oscura. -->
        <item name="android:windowBackground">@color/distop_bars</item>
        <item name="android:statusBarColor">@color/distop_bars</item>
        <item name="android:navigationBarColor">@color/distop_bars</item>
        <item name="android:windowLightStatusBar">@bool/distop_light_bars</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>